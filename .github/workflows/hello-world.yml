name: Hello world workflow

on:
  push:
    branches: [ "terence" ]

jobs:
  Hello-world-job: 
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name : Say hello 
      run  : echo "Hello,  world ! "

